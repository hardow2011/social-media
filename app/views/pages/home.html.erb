<main class="section">
<div class="container">
    <div class="block">
        <h1>Social Media</h1>
        <p>A nice social media app!</p>
    </div>
    <div class="block">
        <% if user_signed_in? %>
            <%= link_to "View posts", posts_path, class: "button is-link" %>
        <% else %>
            <%= link_to "Sign in", new_user_session_path, class: "button" %>
        <% end %>
    </div>
  </div>
</main>